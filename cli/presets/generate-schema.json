{
  "name": "generate-schema",
  "version": "1.0.0",
  "description": "Generate JSON Schema from sample data",
  "steps": [
    {
      "id": "parse",
      "operator": "json.parse",
      "params": {}
    },
    {
      "id": "generate",
      "operator": "schema.generate",
      "params": {
        "draft": "2020-12",
        "required": true,
        "examples": false,
        "title": "Generated Schema"
      }
    },
    {
      "id": "format",
      "operator": "json.stringify",
      "params": {
        "indent": 2
      }
    }
  ],
  "input": {
    "type": "string",
    "format": "json"
  },
  "output": {
    "type": "string",
    "format": "json"
  }
}
